server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: hoy la app es una sola pagina sin rutas propias, pero
    # esto evita un 404 si en el futuro se agrega ruteo del lado del cliente.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
